CREATE TABLE transacciones_a (
    id INT PRIMARY KEY,
    fecha DATE,
    cliente VARCHAR(100),
    monto NUMERIC(10,2)
);

CREATE TABLE transacciones_b (
    id INT PRIMARY KEY,
    fecha DATE,
    cliente VARCHAR(100),
    monto NUMERIC(10,2)
);

